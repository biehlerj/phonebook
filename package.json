{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": "8.16.0"
  },
  "scripts": {
    "start": "node index.js",
    "watch": "nodemon index.js",
    "build:ui": "rm -rf build && cd ~/github.com/biehlerj/fullstackopen_part3_exercises/phonebook-frontend && yarn build --prod && cp -r build ~/github.com/biehlerj/fullstackopen_part3_exercises/phonebook-backend",
    "deploy": "git push heroku master",
    "deploy:full": "yarn build:ui && git add . && git commit -m uibuild && yarn deploy",
    "logs:prod": "heroku logs --tail"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.9.1"
  }
}
